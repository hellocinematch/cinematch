# MovieLens 32M Ingest Handoff

Last updated: 2026-04-07
Owner: Cinemastro app team

## Current status (important)

- We are currently at **Step 4** (seed-user creation track).
- Seed email convention: `seed_user_<id>@seed.cinemastro.local`
- Working folder: `SeedData/ml-32m/`

## 12-step ingest plan

1. Validate schema constraints for ingest
2. Add ingest/perf index
3. Confirm TMDB mapping strategy (`movieId -> tmdbId` via `links.csv`)
4. Create seed users script (Auth + `profiles`) and mapping outputs
5. Pilot seed-user creation
6. Full seed-user creation
7. Verify mapping outputs and failures
8. Build ratings transform job (streamed)
9. Batched upsert into `public.ratings`
10. Pilot ratings ingest + validation
11. Full ratings ingest
12. Post-load QA + production checks

## Status by step

- Step 1: Completed
- Step 2: Completed
- Step 3: Completed
- Step 4: **In progress**
- Step 5-12: Pending

## Key assets

- Dataset files:
  - `SeedData/ml-32m/ratings.csv`
  - `SeedData/ml-32m/links.csv`
  - `SeedData/ml-32m/movies.csv`
  - `SeedData/ml-32m/tags.csv`
- Script:
  - `SeedData/ml-32m/create_seed_users.mjs`
- Mapping outputs (generated by script):
  - `SeedData/ml-32m/user_mapping.csv`
  - `SeedData/ml-32m/user_mapping_failed.csv`

## Transform rules

- Score transform: MovieLens `rating * 2` -> app score (1-10 scale)
- Ratings source for CF remains `public.ratings`
- Do not commit `SeedData/` into git

## What next chat should do first

1. Confirm the current run state of Step 4:
   - users created so far
   - failures so far
   - whether run is still active or completed
2. If Step 4 completes:
   - verify mapping CSV counts
   - proceed to Step 5 pilot verification and Step 8 transform implementation

## Notes

- Optional later: add `profiles.is_seed` and exclude seed users from public stats if needed.
- Keep this file updated with timestamp and progress at each major checkpoint.
